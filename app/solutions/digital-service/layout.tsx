export const metadata = {
  title: "Digital Service | Trigital Tech",
  description:
    "Trigital's digital service solutions: logistics, transport aggregator, healthcare, tour & travel, and home service platforms. Build and launch with NIPIGE.",
};

export default function DigitalServiceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
